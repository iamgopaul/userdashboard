{
  "name": "user-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run dev.ts",
    "dev:server": "cd server && bun run dev",
    "dev:client": "cd client && bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
